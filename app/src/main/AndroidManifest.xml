<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kyudong.agent_client">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LoginNoActionBar">
        <activity
            android:name=".Splash.SplashScreen"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RealMain"></activity>
        <activity android:name=".Login.SignUp" />
        <activity
            android:name=".Board.BoardMain"
            android:label="@string/title_activity_board2"
            android:theme="@style/LoginNoActionBar" />
        <activity android:name=".WriteBoard.BoardWrite" />
        <activity android:name=".ReadBoard.ReadBoard" />
        <activity android:name=".LoginMain" />
        <activity android:name=".Comment.CommentMain"></activity>
    </application>

</manifest>